# 注意事项

- 请使用 **NVM** 安装并切换到 Node.js 版本 20.04。

## 环境建议

- 强烈建议在 **WSL2** 环境下开发（推荐配置：Windows 11 + WSL2 + Ubuntu 22.04），以避免在 Windows 环境下可能遇到的兼容性和性能问题。

## 包管理器

- 必须使用 **pnpm** 作为包管理器，相比于 npm，pnpm 能有效避免 `node_modules` 的嵌套问题。

## 项目运行

- 请务必在本地环境中配置 `.env` 文件，未配置将导致项目无法正常启动。
